{
	"name": "Existing Docker Compose (Extend)",

	"dockerComposeFile": [
		"../compose.yml"
	],

	"service": "ctf-playground",

	"workspaceFolder": "/workspaces",

	"customizations": {
		"vscode": {
			"settings": {
				"terminal.integrated.defaultProfile.linux": "zsh"
			}
		}
	}
}
